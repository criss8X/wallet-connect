{
	"name": "wc-cli",
	"version": "1.0.0",
	"type": "module",
	"bin": {
		"wc-script": "dist/cli.js"
	},
	"files": [
		"dist",
		"templates"
	],
	"scripts": {
		"build": "tsup src/cli.ts --format esm --out-dir dist --clean",
		"prepack": "pnpm run build",
		"start": "pnpm build && cd test_environment && node ../dist/cli.js add wallet-connect -in --direct",
		"dev": "pnpm start",
		"test": "tsup test/test.ts --format esm --out-dir test_dist --clean && node test_dist/test.js"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"devDependencies": {
		"@biomejs/biome": "2.2.4",
		"@types/node": "24.4.0",
		"tsup": "8.5.0",
		"typescript": "5.9.2"
	},
	"dependencies": {
		"zod": "4.1.8"
	}
}