import type { ComponentNeeded, DependenceNeeded } from "./whatsNeed.js";

export async function installDepsNeeded(deps: DependenceNeeded[]) {
	// Improve terminal log
	// Implement ora spinner
}

export async function installComponentsNeeded(components: ComponentNeeded[]) {
	// Improve terminal log
	// Implement ora spinner
}
