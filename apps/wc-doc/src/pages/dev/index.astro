---
import "@/styles/global.css";
import connectWalletComp from "@wcs/components/ConnectWallet?raw";
import background from "public/background.webp";
import favicon from "public/favicon.svg";
import CodeSnippet from "@/components/code.astro";
import {
  Github,
  Linkedin,
  Palette,
  Rocket,
  Shield,
  Sparkles,
} from "@/components/icon.astro";
import Example from "@/sections/example.astro";
import UsesIn from "@/sections/usesIn.astro";

const features = [
  {
    name: "Conexión instantánea",
    icon: Rocket,
    description:
      "Vincula tu wallet en segundos con una experiencia moderna y sencilla.",
  },
  {
    name: "Basado en Shadcn",
    icon: Shield,
    description:
      "El componente está basado en shadcn, permitiendo la reutilización de componentes.",
  },
  {
    name: "Compatible con múltiples wallets",
    icon: Sparkles,
    description:
      "Funciona con MetaMask, WalletConnect, Coinbase Wallet y muchas más.",
  },
  {
    name: "Totalmente personalizable",
    icon: Palette,
    description:
      "Ajusta el diseño y las funciones según las necesidades de tu proyecto.",
  },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={favicon.src} />
    <meta name="viewport" content="width=device-width" />

    <meta
      name="description"
      content="A shadcn-based component for wallet connections with wagmi & viem."
    />

    <title>Dev</title>
  </head>

  <body class="h-screen overflow-y-auto bg-neutral-950 space-y-8">
    <img
      src={background.src}
      alt="App background"
      class="fixed opacity-20 -z-50 size-full"
    />

    <header
      class="sticky top-0 border-b border-b-neutral-900 h-20 w-full bg-neutral-950/50 backdrop-blur-xl"
    >
      <div
        class="max-w-6xl mx-auto flex items-center justify-between size-full xs:px-2 lg:px-4"
      >
        <h1>Wallet Connect</h1>

        <div class="flex gap-2">
          <button>Github</button>

          <button>Preview</button>
        </div>
      </div>
    </header>

    <main class="max-w-6xl mx-auto pb-12">
      <div class="space-y-12">
        <div class="space-y-4">
          <div class="flex gap-4 items-center">
            <div class="size-14 bg-neutral-900 rounded-md"></div>

            <div>
              <h1 class="text-3xl">Wallet Connect</h1>

              <span>
                Integra wallets de criptomonedas en tu app de forma rápida y
                segura.
              </span>
            </div>
          </div>

          <div class="flex gap-4 flex-wrap">
            {
              ["Typescript", "React", "Wagmi & Viem"].map((tech) => (
                <span class="bg-neutral-800 px-2 py-0.5 rounded-md text-sm">
                  {tech}
                </span>
              ))
            }
          </div>

          <div class="max-w-4xl grid grid-cols-2 grid-rows-2 gap-4">
            {
              features.map(({ name, description, icon: Icon }) => (
                <div class="h-36 border border-neutral-800 rounded-md p-4 flex flex-col justify-between py-6 from-neutral-700/10 to-transparent bg-gradient-to-t">
                  <div class="flex gap-2">
                    <div class="flex items-center justify-center bg-neutral-800 size-8 rounded-md">
                      <Icon class="size-5" />
                    </div>

                    <h2 class="font-mono text-xl mb-2 leading-relaxed">
                      {name}
                    </h2>
                  </div>

                  <p class="text-neutral-400 text-sm">{description}</p>
                </div>
              ))
            }
          </div>
        </div>

        <UsesIn className="space-y-4" />

        <div class="space-y-4">
          <h1 class="text-2xl">Instalación</h1>

          <span>
            Un componente listo para integrar: copia el código, crea el archivo
            y úsalo en tu proyecto. Puedes personalizarlo y reutilizar sus
            partes internas según tus necesidades.
          </span>

          <div class="mt-4">
            <CodeSnippet title="ConnectWallet.tsx" code={connectWalletComp} />
          </div>
        </div>

        <Example className="space-y-4" />
      </div>
    </main>
  </body>
</html>
